"use client";

/**
 * @deprecated Prefer `VaultEditor` from `@/editor`.
 * Kept as a thin alias so existing imports keep working.
 */
export { VaultEditor as BlockEditor } from "@/editor/vault-editor";
